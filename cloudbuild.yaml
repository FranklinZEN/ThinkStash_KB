steps:
# Step 1: Install Dependencies
- name: 'gcr.io/cloud-builders/npm' # Using the dedicated npm builder
  entrypoint: 'npm'
  args: ['install']
  id: 'Install Dependencies'

options:
  logging: CLOUD_LOGGING_ONLY
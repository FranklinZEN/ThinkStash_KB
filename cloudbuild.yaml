# This is a complete refactor of the cloudbuild.yaml for the new architecture.
# It separates build steps for the webapp and the new AI worker service.

substitutions:
  _GCP_REGION: us-central1
  # Add other substitutions like _PROJECT_ID, _ARTIFACT_REGISTRY_REPO etc. as needed from your setup.
  # Assuming _DB_INSTANCE_CONNECTION_NAME, _CLOUD_RUN_SERVICE_NAME, _CLOUD_RUN_SA_EMAIL are set in the trigger.

steps:
# ==============================================================================
# WEBAPP - Build, Test, and Deploy the Next.js Frontend
# ==============================================================================

# 1. Install Webapp Dependencies
- name: 'node:20'
  id: 'webapp-install'
  entrypoint: 'npm'
  args: ['install']

# 2. Generate Prisma Client
# This is a build-time dependency for the Next.js app.
- name: 'node:20'
  id: 'webapp-prisma-generate'
  waitFor: ['webapp-install']
  entrypoint: 'npx'
  args: ['prisma', 'generate']

# 3. Build Next.js App
- name: 'node:20'
  id: 'webapp-build'
  waitFor: ['webapp-prisma-generate']
  entrypoint: 'npm'
  args: ['run', 'build']

# 4. Build Webapp Docker Image
- name: 'gcr.io/cloud-builders/docker'
  id: 'webapp-docker-build'
  waitFor: ['webapp-build']
  args: [
    'build',
    '--no-cache', # Add this flag to force a fresh build and prevent stale UI issues
    '-t',
    '${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-webapp:$COMMIT_SHA',
    '-f', 'Dockerfile', # Explicitly use the root Dockerfile
    '.'
  ]

# 5. Push Webapp Docker Image
- name: 'gcr.io/cloud-builders/docker'
  id: 'webapp-docker-push'
  waitFor: ['webapp-docker-build']
  args: ['push', '${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-webapp:$COMMIT_SHA']

# 6. Run Webapp Migrations
# This step uses the newly built webapp image to run prisma migrations.
- name: '${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-webapp:$COMMIT_SHA'
  id: 'webapp-migrations'
  waitFor: ['webapp-docker-push']
  secretEnv: ['DATABASE_URL']
  entrypoint: 'sh'
  args:
    - '-c'
    - |
      echo "Running Prisma migrations..."
      npx prisma migrate deploy
      echo "Migrations complete."

# 7. Deploy Webapp to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  id: 'webapp-deploy'
  waitFor: ['webapp-migrations']
  entrypoint: gcloud
  args: [
    'run', 'deploy', '${_CLOUD_RUN_SERVICE_NAME}', # e.g., thinkstash-webapp
    '--image=${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-webapp:$COMMIT_SHA',
    '--region=${_GCP_REGION}',
    '--platform=managed',
    '--allow-unauthenticated',
    '--service-account=${_CLOUD_RUN_SA_EMAIL}',
    # IMPORTANT: Mount the database URL and NextAuth secrets for the webapp
    '--set-secrets=DATABASE_URL=thinkstash-db-url:latest,NEXTAUTH_SECRET=nextauth-secret:latest'
  ]

# ==============================================================================
# AISERVICE - Build and Deploy the Python Worker
# ==============================================================================

# 8. Build AI Worker Docker Image
- name: 'gcr.io/cloud-builders/docker'
  id: 'worker-docker-build'
  args: [
    'build',
    '-t',
    '${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-worker:$COMMIT_SHA',
    '-f', 'aiservice/worker.Dockerfile',
    '.'
  ]

# 9. Push AI Worker Docker Image
- name: 'gcr.io/cloud-builders/docker'
  id: 'worker-docker-push'
  waitFor: ['worker-docker-build']
  args: ['push', '${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-worker:$COMMIT_SHA']

# 10. Deploy AI Worker to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  id: 'worker-deploy'
  waitFor: ['worker-docker-push']
  entrypoint: gcloud
  args: [
    'run', 'deploy', 'thinkstash-worker', # Hardcoded service name as per plan
    '--image=${_GCP_REGION}-docker.pkg.dev/${_PROJECT_ID}/${_ARTIFACT_REGISTRY_REPO}/thinkstash-worker:$COMMIT_SHA',
    '--region=${_GCP_REGION}',
    '--platform=managed',
    '--no-allow-unauthenticated', # Worker should not be publicly accessible
    '--min-instances=1', # Keep one instance warm to process tasks quickly
    '--max-instances=5',
    '--cpu=1',
    '--memory=512Mi',
    '--service-account=${_CLOUD_RUN_SA_EMAIL}',
    # IMPORTANT: Mount the database URL secret for the worker
    '--set-secrets=DATABASE_URL=thinkstash-db-url:latest'
  ]

# Define available secrets from Secret Manager
availableSecrets:
  secretManager:
  # This secret provides the full DATABASE_URL for Prisma and the worker
  - versionName: projects/${_PROJECT_ID}/secrets/thinkstash-db-url/versions/latest
    env: 'DATABASE_URL'

options:
  logging: CLOUD_LOGGING_ONLY


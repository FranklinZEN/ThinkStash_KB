version: '3.8' # Specify compose file version

services:
  postgres:
    image: postgres:15 # Use PostgreSQL version 15
    container_name: knowledge-cards-db # Name the container
    environment:
      POSTGRES_USER: user # Default username from .env
      POSTGRES_PASSWORD: password # Default password from .env
      POSTGRES_DB: knowledge_cards # Default database name from .env
    ports:
      - "5433:5432" # Map host port 5433 to container port 5432
    volumes:
      - postgres_data:/var/lib/postgresql/data # Persist data using a named volume
    restart: always # Always restart the container if it stops

  redis:
    image: redis:alpine
    container_name: thinkstash-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  aiservice:
    build:
      context: .
      dockerfile: aiservice/Dockerfile
    container_name: thinkstash-aiservice
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    env_file:
      - ./aiservice/.env
    restart: always
    depends_on:
      - postgres
      - redis

  worker:
    build:
      context: .
      dockerfile: aiservice/worker.Dockerfile
    container_name: thinkstash-worker
    env_file:
      - ./aiservice/.env
    restart: always
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data: # Define the named volume
  redis_data: 